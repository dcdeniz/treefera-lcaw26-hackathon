# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
out/
.next/
.cache/

# Env / secrets
.env
.env.*
!.env.example
.dev.vars
*.pem

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Misc
.wrangler/
coverage/

# Large data (downloaded, not versioned)
# Anchored with a leading slash so these match ONLY the top-level Drive downloads.
# An unanchored `data/` also matches solution/webapp/public/data/ (the static demo
# bundle), which MUST ship with the repo for the demo to render (INFRA_CONTRACT §4.1).
/real-data/
/data/
solution/.venv/
.venv/

# Live credentials — never commit (see conversations/0004)
SECRETS.md
